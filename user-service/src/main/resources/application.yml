server:
  port: ${SERVER_PORT:8081}

spring:
  application:
    name: user-service
  datasource:
    url: ${MYSQL_URL:jdbc:mysql://localhost:3306/user_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true}
    username: ${MYSQL_USER:user_app}
    password: ${MYSQL_PASSWORD:user_secret}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

nats:
  url: ${NATS_URL:nats://localhost:4222}
  username: ${NATS_USERNAME:app_user}
  password: ${NATS_PASSWORD:nats_secret}
  stream-name: ${NATS_STREAM:USER_EVENTS}
  subject: ${NATS_SUBJECT:events.user}

security:
  jwt:
    secret: ${JWT_SECRET:change-me-to-a-long-random-secret-key-at-least-32-chars}
    expiration-seconds: ${JWT_EXPIRATION_SECONDS:3600}
  gateway-token: ${GATEWAY_TOKEN:gateway-internal-token}

management:
  endpoints:
    web:
      exposure:
        include: health,info
